# Local Workflow Logs

Project and experiment logs are intentionally not tracked in this public repository.

Keep machine-specific logs locally, for example:

- `docs/project-plan-log.md`
- `docs/experiment-log.md`

Those files may contain local paths, hostnames, experiment outputs, operational history, and other private context. Publish only sanitized summaries or templates.
