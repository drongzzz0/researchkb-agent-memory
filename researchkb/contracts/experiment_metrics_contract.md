# Experiment Metrics Contract

Purpose: make experiment outputs harvestable, comparable, and useful to research agents.

Every experiment output directory should include at least one parseable artifact:

- `metrics.json`
- `results.json`
- `summary.json`
- `eval_results.json`

The preferred artifact is `metrics.json`.

## Minimum JSON Shape

```json
{
  "project": "Project name",
  "experiment": "short_unique_name",
  "run_id": "stable_run_id",
  "status": "running | completed_positive | completed_negative | failed",
  "git_commit": "",
  "config_path": "",
  "dataset": "",
  "model": "",
  "seed": "",
  "metrics": {},
  "failure_type": "",
  "decision": "continue | stop | redesign | rerun | unknown",
  "next_action": "",
  "artifacts": [],
  "notes": ""
}
```

## Required Fields

| Field | Required | Description |
| --- | --- | --- |
| `project` | yes | Project or research direction |
| `experiment` | yes | Human-readable experiment name |
| `run_id` | recommended | Stable ID for later comparison |
| `status` | yes | Run outcome |
| `dataset` | recommended | Dataset, benchmark, or sample description |
| `model` | recommended | Model or system under test |
| `metrics` | yes | Parsed metric dictionary |
| `failure_type` | if failed | Short reusable failure label |
| `decision` | recommended | `continue`, `stop`, `redesign`, `rerun`, or `unknown` |
| `next_action` | recommended | One concrete follow-up action |
| `artifacts` | recommended | Files generated by the run |

## Status Values

- `running`: run has started but no final judgement exists.
- `completed_positive`: run supports continuing or strengthening the current direction.
- `completed_negative`: run completed but argues against the current direction or setup.
- `failed`: run crashed, timed out, produced NaN, OOM, malformed output, or another operational failure.

## Decision Values

- `continue`: keep the current direction.
- `stop`: stop this branch.
- `redesign`: change the method, setup, or assumption.
- `rerun`: repeat because evidence is inconclusive or noisy.
- `unknown`: no decision yet.

## Plain Log Fallback

If JSON is not available, emit parseable metric lines:

```text
METRIC accuracy=0.842
METRIC latency_ms=128.5
METRIC peak_memory_mb=9216
METRIC failure_type=timeout
METRIC decision=rerun
```

## Health Targets

- `with_metrics / experiment_runs >= 0.70`
- every failed run should include `failure_type`
- every important run should include a decision and next action
- publishable runs should include enough metrics to compare quality, cost, speed, and reliability

## Agent Use

A research agent should be able to answer:

- What changed in this run?
- Which metric improved or regressed?
- Is this run evidence for `continue`, `stop`, `redesign`, or `rerun`?
- What evidence is missing?
- What is the next concrete experiment?
